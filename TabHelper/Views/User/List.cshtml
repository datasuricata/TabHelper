@model TabHelper.Models.ViewModel.UserViewModel

<div class="panel panel-default">
    <div class="panel-heading">Listagem de Usuários</div>
    <div class="panel-body">

        <table class="table table-striped table-hover text-center">
            <thead>
                <tr>
                    <th class="text-center">#</th>
                    <th class="text-center">Data Criação</th>
                    <th class="text-center">Nome</th>
                    <th class="text-center">E-mail</th>
                    <th class="text-center">Ação</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null)
                {
                    foreach (var user in Model.Users)
                    {
                        <tr>
                            <td>@user.Id</td>
                            <td>@user.CreatedAt?.ToString("dd/MM/yyyy HH:mm")</td>
                            <td>@user.Name</td>
                            <td>@user.Email</td>
                            <td>
                                @if (user.IsDeleted)
                                {
                                    <a asp-controller="User" asp-action="Block" asp-route-id="@user.Id" class="btn btn-warning btn-xs" style="width: 80px;" data-toggle="modal" data-target="#block">Desbloquear</a>
                                }
                                else
                                {
                                    <a asp-controller="User" asp-action="Block" asp-route-id="@user.Id" class="btn btn-default btn-xs" style="width: 80px;" data-toggle="modal" data-target="#block">Bloquear</a>
                                }

                                <a asp-controller="User" asp-action="Edit" asp-route-id="@user.Id" style="width: 80px;" class="btn btn-primary btn-xs" data-toggle="modal" data-target="#edit">Editar</a>

                                <a asp-controller="User" asp-action="Delete" asp-route-id="@user.Id" class="btn btn-danger btn-xs" style="width: 80px;" data-toggle="modal" data-target="#delete">Deletar</a>
                            </td>
                        </tr>
                    }
                }

            </tbody>
        </table>
    </div>
</div>