@model TabHelper.Models.ViewModel.DepartmentModel

@{
    Layout = null;
}

<div class="modal-content">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">Deletar departamento</h4>
    </div>
    <div class="modal-body">
        <form data-ajax="true" data-ajax-method="POST" data-ajax-failure="formOnFail" data-ajax-success="window.location = '/Index'" asp-controller="Department" asp-action="Delete" class="form-horizontal">
            <div class="well text-center" style="border-color:#029acf">
                <strong>Deseja deletar o departamento <b style="color:brown">@Model.Name</b>?</strong>
            </div>
            <div class="well text-center" style="border-color:#029acf">
                <fieldset>
                    <div class="form-group">
                        <div class="form-group">
                            <div class="col-lg-12 text-center">
                                <input type="hidden" asp-for="Id" />
                                Os formulários linkados ao departamento serão desvinculados deseja continuar?
                            </div>
                        </div>
                    </div>
                </fieldset>
            </div>
            <div class="text-right">
                <button type="button" class="btn btn-default btn-sm" data-dismiss="modal">Cancelar</button>
                <button type="submit" class="btn btn-info btn-sm">Confirmar</button>
            </div>
        </form>
    </div>
    <div class="modal-footer">
    </div>
</div>